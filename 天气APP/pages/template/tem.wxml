<template name="future-item">
  <view class="future-item">
    <view>{{item.date}}</view>
    <view>{{item.high}}</view>
    <view>{{item.fengli}}</view>
    <view>{{item.low}}</view>
    <view>{{item.type}}</view>
  </view>
</template>